## 言語指定

このスキルの指示は英語で書かれていますが、ユーザーへの応答はすべて日本語で行ってください。技術用語は英語のままで構いません。

## マージ済みブランチの削除判定

リモートでマージ済みのブランチは、確認なしで自動削除してよい。

ローカルのみでマージ済みのブランチは、削除前にユーザーに確認する。

worktree 付きブランチは、worktree が clean であれば削除候補に含めてよいが、削除実行前には必ず確認する。
