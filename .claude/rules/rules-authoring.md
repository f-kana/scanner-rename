---
paths:
  - ".claude/rules/**"
---

# rules 作成・編集のチェックリスト

- paths フロントマターを付けること。常時ロードが必要な場合のみ省略する
- paths はルールが効くべきファイル種別まで絞ること（例: `docs/**` ではなく `docs/**/*.html`）
- 内容はそのファイルを編集する人が判断に必要な情報だけにすること。仕組みの裏側（ビルドスクリプトの詳細等）は書かない
